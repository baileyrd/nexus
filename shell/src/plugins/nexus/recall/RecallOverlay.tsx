// shell/src/plugins/nexus/recall/RecallOverlay.tsx
//
// BL-044 — placeholder overlay component (commit 1 scaffold). The
// full UI lands in commit 2; this stub renders nothing so the plugin
// activates cleanly while the runtime + store land first.

export function RecallOverlay() {
  return null
}
