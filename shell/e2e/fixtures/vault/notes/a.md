# Alpha

## Section one

A short paragraph introducing the topic. Written for the outline-update
assertions — any heading count / text mutation the harness performs must
not accidentally match this baseline.

## Section two

Another paragraph with a [[b]] wiki-link so the backlinks plugin sees
this file as a source when viewing `b.md`.
