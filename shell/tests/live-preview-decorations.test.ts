/**
 * Re-export wrapper so the default `pnpm test` glob picks up the
 * live-preview decoration tests that live as a sibling of the
 * implementation under
 * `shell/src/plugins/nexus/editor/cm/livePreviewDecorations.test.ts`.
 */
import '../src/plugins/nexus/editor/cm/livePreviewDecorations.test.ts'
