{
  "name": "{{plugin-id}}",
  "version": "0.1.0",
  "description": "{{description}}",
  "author": "{{author}}",
  "private": true,
  "type": "module",
  "main": "index.js",
  "scripts": {
    "build": "esbuild index.ts --bundle --format=esm --target=es2020 --outfile=index.js",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@nexus/extension-api": "{{extension-api-version}}",
    "esbuild": "^0.24.0",
    "typescript": "^5.5.0"
  }
}
