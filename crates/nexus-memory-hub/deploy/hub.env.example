# Environment for the Nexus memory sync hub. Copy to hub.env and fill in.
# Keep it private (chmod 600) — this secret authenticates every client.

# Shared bearer token every client must present. Generate with:
#   openssl rand -hex 32
SYNC_SECRET=replace-me-with-openssl-rand-hex-32
