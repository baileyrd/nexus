{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "AcpRegisterServerReply",
  "description": "Reply for `register_server` (handler `6`). `status` is one of:\n- `\"ok\"` — adapter registered.\n- `\"already_registered\"` — another contribution owns the `name`.\n- `\"invalid_name\"` / `\"invalid_command\"` — required field was\n  empty / whitespace-only.",
  "type": "object",
  "properties": {
    "ok": {
      "description": "`true` iff the adapter was inserted (status == `\"ok\"`).",
      "type": "boolean"
    },
    "status": {
      "description": "One of `\"ok\"`, `\"already_registered\"`, `\"invalid_name\"`,\n`\"invalid_command\"`.",
      "type": "string"
    }
  },
  "additionalProperties": false,
  "required": [
    "ok",
    "status"
  ]
}
