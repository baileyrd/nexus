{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "StorageBaseRecordCreateArgs",
  "description": "Args for `com.nexus.storage::base_record_create`.",
  "type": "object",
  "properties": {
    "path": {
      "description": "Forge-relative path of the `.bases/` directory.",
      "type": "string"
    },
    "record": {
      "description": "`nexus_types::bases::BaseRecord` serialised as JSON. The\nhandler runs `serde_json::from_value::<BaseRecord>` on this\nfield, so malformed inner shapes still surface as parse\nerrors — just inside the handler, not at the envelope."
    }
  },
  "additionalProperties": false,
  "required": [
    "path",
    "record"
  ]
}
