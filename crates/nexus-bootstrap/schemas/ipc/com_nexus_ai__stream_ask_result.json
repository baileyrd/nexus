{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "AiStreamAskResult",
  "description": "Return type for `com.nexus.ai::stream_ask`. Mirrors the final\n`stream_done` event payload and is what the IPC call resolves to when\nthe stream completes.",
  "type": "object",
  "properties": {
    "session_id": {
      "description": "Session id — matches the `session_id` on every `stream_chunk`\nevent. Generated by the handler when the caller omitted it.",
      "type": "string"
    },
    "sources": {
      "description": "RAG sources that grounded the answer, in retrieval order.",
      "type": "array",
      "items": {
        "$ref": "#/$defs/AiStreamAskSource"
      }
    },
    "text": {
      "description": "Full aggregated assistant response.",
      "type": "string"
    }
  },
  "required": [
    "session_id",
    "text",
    "sources"
  ],
  "$defs": {
    "AiStreamAskSource": {
      "description": "One retrieved source chunk used to ground the answer. Mirror of\n[`crate::vectorstore::ChunkMatch`].",
      "type": "object",
      "properties": {
        "block_id": {
          "description": "Identifier of the originating block.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "chunk_text": {
          "description": "Textual content of the chunk.",
          "type": "string"
        },
        "file_path": {
          "description": "Path of the source file.",
          "type": "string"
        },
        "score": {
          "description": "Cosine similarity score (higher is more relevant).",
          "type": "number",
          "format": "float"
        }
      },
      "required": [
        "file_path",
        "block_id",
        "chunk_text",
        "score"
      ]
    }
  }
}
