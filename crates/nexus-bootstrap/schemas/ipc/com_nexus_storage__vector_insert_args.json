{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "StorageVectorInsertArgs",
  "description": "Args for `com.nexus.storage::vector_insert`. Replaces all chunks\nfor `file_path` atomically.",
  "type": "object",
  "properties": {
    "chunks": {
      "description": "Replacement chunk set. Empty array clears all chunks for the\nfile without inserting new ones.",
      "type": "array",
      "items": {
        "$ref": "#/$defs/StorageChunkEmbedding"
      }
    },
    "file_path": {
      "description": "Forge-relative path of the source file. Used as the dedup key\n(scoped to `namespace`).",
      "type": "string"
    },
    "namespace": {
      "description": "Collection the vectors belong to (e.g. `notes`, `memory`). Defaults to\n`notes` so existing callers that omit it keep their current behaviour.",
      "type": "string",
      "default": "notes"
    }
  },
  "additionalProperties": false,
  "required": [
    "file_path",
    "chunks"
  ],
  "$defs": {
    "StorageChunkEmbedding": {
      "description": "Mirror of [`crate::vectorstore::ChunkEmbedding`] — kept in sync\nmanually so the impl type stays free of the optional `ts-rs` /\n`schemars` derives. Compared via `cargo test -p nexus-bootstrap\n--test ipc_schema_emit`.",
      "type": "object",
      "properties": {
        "block_id": {
          "description": "Identifier of the originating block.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "chunk_text": {
          "description": "Textual content of the chunk.",
          "type": "string"
        },
        "embedding": {
          "description": "Dense vector representation of the chunk.",
          "type": "array",
          "items": {
            "type": "number",
            "format": "float"
          }
        },
        "file_path": {
          "description": "Path of the source file (forge-relative).",
          "type": "string"
        }
      },
      "additionalProperties": false,
      "required": [
        "file_path",
        "block_id",
        "chunk_text",
        "embedding"
      ]
    }
  }
}
