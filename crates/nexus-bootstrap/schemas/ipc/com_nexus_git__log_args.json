{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "GitLogArgs",
  "description": "Args for `com.nexus.git::log` (handler id `2`).",
  "type": "object",
  "properties": {
    "limit": {
      "description": "Maximum number of entries to return, newest first. Omit for\nthe default of 20.",
      "type": [
        "integer",
        "null"
      ],
      "format": "uint64",
      "minimum": 0
    }
  },
  "additionalProperties": false
}
