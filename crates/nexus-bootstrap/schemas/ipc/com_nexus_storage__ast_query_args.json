{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "StorageAstQueryArgs",
  "description": "Args for `com.nexus.storage::ast_query` (handler id `75`).\n\nRuns a [tree-sitter query] over the forge's code files of one `language`.\nPhase 5.2 / RFC 0005.\n\n[tree-sitter query]: https://tree-sitter.github.io/tree-sitter/using-parsers#query-syntax",
  "type": "object",
  "properties": {
    "language": {
      "description": "Grammar to parse with: `rust`, `typescript`, `tsx`, `javascript`,\n`jsx`, `python`, or `go`. Only files of this language are searched.",
      "type": "string"
    },
    "max_results": {
      "description": "Maximum matches to return. Defaults to 100.",
      "type": [
        "integer",
        "null"
      ],
      "format": "uint32",
      "minimum": 0
    },
    "path": {
      "description": "Optional forge-relative file or directory prefix to scope the search.",
      "type": [
        "string",
        "null"
      ]
    },
    "query": {
      "description": "A tree-sitter query (S-expression pattern with `@capture`s).",
      "type": "string"
    }
  },
  "additionalProperties": false,
  "required": [
    "language",
    "query"
  ]
}
