{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "EntityUpsertArgs",
  "description": "Args for `com.nexus.storage::entity_upsert` (handler 67).\n\n`id` becomes the markdown file stem under `<forge>/entities/`.\nAll other fields map directly to the on-disk YAML keys recognised\nby the thin-slice parser. Existing files are overwritten via the\natomic-write path (temp-fsync-rename) so a concurrent read\nobserves either the old or the new content, never a torn write.",
  "type": "object",
  "properties": {
    "aliases": {
      "description": "`aliases:` frontmatter key. Empty list omits the field on disk.",
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "description": {
      "description": "`description:` frontmatter key. Empty string omits the field.",
      "type": "string"
    },
    "entity_type": {
      "description": "`entity_type:` frontmatter key.",
      "type": "string"
    },
    "id": {
      "description": "Canonical id — becomes the markdown file stem.",
      "type": "string"
    },
    "relations": {
      "description": "`relations:` frontmatter list. Empty list omits the field.",
      "type": "array",
      "items": {
        "$ref": "#/$defs/EntityUpsertRelationRow"
      }
    }
  },
  "additionalProperties": false,
  "required": [
    "id",
    "entity_type"
  ],
  "$defs": {
    "EntityUpsertRelationRow": {
      "description": "One outgoing relation entry inside [`EntityUpsertArgs::relations`].\nRelation kinds are normalised through\n`crate::entity_index::normalize_relation_type` before being written\nto disk, so callers can submit free-form LLM output without\npreprocessing.",
      "type": "object",
      "properties": {
        "confidence": {
          "description": "Confidence in `[0.0, 1.0]`. Absent ⇒ `1.0` on disk.",
          "type": [
            "number",
            "null"
          ],
          "format": "float"
        },
        "target": {
          "description": "Target entity id (or alias — preserved verbatim).",
          "type": "string"
        },
        "type": {
          "description": "Free-form relation kind. Normalised before persistence.",
          "type": "string"
        }
      },
      "additionalProperties": false,
      "required": [
        "target",
        "type"
      ]
    }
  }
}
