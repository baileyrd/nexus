# Capabilities

> **As of:** 2026-05-22. Sources: `crates/nexus-plugin-api/src/capability.rs` (the `Capability` enum), `crates/nexus-security/src/risk.rs` (risk classification), `crates/nexus-bootstrap/cap_matrix.toml` (per-handler usage). Live mirror at [`../generated/capabilities.md`](../generated/capabilities.md) — regenerated by `scripts/check_ipc_drift.sh`.

## Inventory (33 total)

The kernel grants capabilities at IPC dispatch. A plugin's `manifest.capabilities.required` / `optional` declares what it asks for; the install-time UI shows the user the HIGH-risk ones and persists the grant under `<plugin_dir>/granted_caps.json` (encrypted under `chacha20poly1305`).

| String | Variant | Risk | Where it gates |
|--------|---------|------|----------------|
| `fs.read` | `FsRead` | Low | `nexus-storage` read paths |
| `fs.write` | `FsWrite` | Medium | `nexus-storage` write paths (forge-relative) |
| `fs.read.external` | `FsReadExternal` | **High** | `nexus-storage` reads that resolve outside the forge root |
| `fs.write.external` | `FsWriteExternal` | **High** | `nexus-storage` writes that resolve outside the forge root |
| `net.http` | `NetHttp` | **High** | `nexus-ai` providers, `nexus-linkpreview`, `nexus-notifications` (webhooks/SMTP), `nexus-collab` (relay client), `nexus-audio` (provider backend), `nexus-git::{push, push_tags}` |
| `net.http.localhost` | `NetHttpLocalhost` | Medium | Ollama, local llama.cpp |
| `process.spawn` | `ProcessSpawn` | **High** | `com.nexus.terminal::{create_session, repl_start, send_input, send_raw_input, run_saved, adhoc_promote, repl_eval}`, `com.nexus.mcp.host::connect`, `com.nexus.dap::launch` / `attach`, `com.nexus.acp::initialize` |
| `kv.read` | `KvRead` | Low | KV trait reads — `nexus-agent` memory, plugin settings |
| `kv.write` | `KvWrite` | Low | KV trait writes |
| `ipc.call` | `IpcCall` | **High** | Unconditional check on every `context.ipc_call(...)` |
| `db.query` | `DbQuery` | Medium | Reserved — bases SQL via storage |
| `db.write` | `DbWrite` | Medium | Reserved — bases SQL via storage |
| `events.publish` | `EventsPublish` | Medium | `ctx.publish(...)` — plugins publishing on the kernel bus |
| `ui.notify` | `UiNotify` | Low | `com.nexus.notifications::send` downstream transport gate |
| `ai.chat` | `AiChat` | Medium | `com.nexus.ai::{stream_chat, stream_ask, ask, semantic_search, enrich_file, enrich_entity, infer_entity_relations, propose_tool_calls, generate_docs, predict}` and `com.nexus.agent::{session_run, round_decide, delegate, plan}` |
| `ai.index` | `AiIndex` | Low | `com.nexus.ai::index_file`, `index_trigger` |
| `ai.session.read` | `AiSessionRead` | Low | `com.nexus.ai::session_load`, `session_list` |
| `ai.session.write` | `AiSessionWrite` | Low | `com.nexus.ai::session_save`, `session_delete` |
| `ai.config.write` | `AiConfigWrite` | **High** | `com.nexus.ai::set_config` — hot-swap provider credentials |
| `ai.activity.write` | `AiActivityWrite` | Medium | `com.nexus.ai::activity_clear` |
| `ai.tools.write` | `AiToolsWrite` | Medium | Stacked via `ai_tools_policy` on `stream_chat`/`propose_tool_calls` when `tools=auto` |
| `ai.tools.mcp` | `AiToolsMcp` | Medium | Stacked via `ai_tools_policy` when `tools=auto_with_mcp` |
| `audio.record` | `AudioRecord` | **High** | `com.nexus.audio::transcribe` |
| `audio.synthesize` | `AudioSynthesize` | Low | `com.nexus.audio::synthesize` |
| `ai.runtime.submit` | `AiRuntimeSubmit` | Medium | `com.nexus.ai.runtime::submit` |
| `ai.runtime.control` | `AiRuntimeControl` | Medium | `com.nexus.ai.runtime::cancel`, `pause`, `resume` |
| `ai.runtime.observe` | `AiRuntimeObserve` | Low | `com.nexus.ai.runtime::get`, `list`, `events`, `pool_stats`, `wait_for` |
| `notifications.inbox.read` | `NotificationsInboxRead` | Low | `com.nexus.notifications::inbox_list`, `inbox_stats` |
| `notifications.inbox.write` | `NotificationsInboxWrite` | Low | `com.nexus.notifications::inbox_mark_read`, `inbox_dismiss` |
| `protocol.host.contribute` | `ProtocolHostContribute` | **High** | The DAP/LSP/MCP-host/ACP `register_*` and `unregister_*` lifecycle verbs — invoker-only via `TrustLevel::Core => Capability::ALL`. Blocks community plugins from forging contributions records. |
| `security.write` | `SecurityWrite` | **High** | `com.nexus.security::{set_secret, delete_secret}` — keyring writes |
| `security.audit.write` | `SecurityAuditWrite` | **High** | `com.nexus.security::clear_audit_log` — log truncation |
| `network.bind` | `NetworkBind` | **High** | `com.nexus.collab::start_relay` — opens a 0.0.0.0 listener so other peers can join the in-process collaboration relay |

## Risk classification

`crates/nexus-security/src/risk.rs::risk_level(Capability) -> RiskLevel`.

- **High** (orange/red in install-time UI): `fs.{read,write}.external`, `net.http`, `process.spawn`, `ipc.call`, `ai.config.write`, `audio.record`, `protocol.host.contribute`, `security.write`, `security.audit.write`, `network.bind`.
- **Medium**: `fs.write`, `net.http.localhost`, `db.{query,write}`, `events.publish`, `ai.chat`, `ai.activity.write`, `ai.tools.{write,mcp}`, `ai.runtime.{submit,control}`.
- **Low**: `fs.read`, `kv.{read,write}`, `ui.notify`, `ai.{index, session.read, session.write}`, `ai.runtime.observe`, `audio.synthesize`, `notifications.inbox.{read,write}`.

## Design rationale: granularity vs aggregation

The vocabulary is deliberately fine-grained. A handful of caps are "singletons" — gating exactly one handler today — and several `*.write` caps have no `*.read` peer. Both shapes are intentional:

### Singletons gate by sensitivity, not by handler count

`ai.config.write`, `ai.activity.write`, `ai.runtime.submit`, `audio.record`, `audio.synthesize`, `network.bind`, `security.audit.write` each gate exactly one handler today. They are not folded into broader caps because each gates a distinct sensitivity class:

| Singleton | Why it stays alone |
|-----------|---------------------|
| `ai.config.write` | Hot-swap of provider credentials. HIGH risk by ADR 0022 — equivalent in surface area to `process.spawn`. Folding into `ai.chat` would let any chat-capable plugin rotate provider keys silently. |
| `ai.activity.write` | Truncates the AI-activity log. Distinct from chat use (the activity log is the operator's record of what the AI was asked to do). |
| `ai.runtime.submit` | Distinct from `ai.runtime.observe` and `ai.runtime.control` so a read-only observability panel can be wired with the smaller grant. ADR 0028. |
| `audio.record` vs `audio.synthesize` | Mic capture is HIGH risk (ambient room audio exfiltration); speaker output is Low risk. A user might want TTS but never the mic — collapsing them into a single `audio.use` would force a worst-case grant for the TTS-only case. BL-117. |
| `network.bind` | Binds a 0.0.0.0 listener for collab relays. Distinct from `net.http` (outbound) because inbound and outbound have different threat models. P1-07. |
| `security.audit.write` | Log truncation — the surface a hostile caller would target to cover tracks. Distinct from `security.write` (keyring) so a settings panel managing keyring entries cannot also erase audit history. P1-01. |

A new handler that shares a sensitivity class with an existing singleton should reuse the singleton rather than spawn a parallel cap.

### Read/write asymmetry: reads stay unrestricted when they don't leak secrets

Three handler pairs have a gated write and an `unrestricted` read:

| Pair | Write gate | Read classification | Why reads are unrestricted |
|------|-----------|---------------------|-----------------------------|
| `ai::set_config` / `ai::config` | `ai.config.write` | `unrestricted` | `ai::config` returns the active provider config **with secrets stripped** — the settings UI needs read access at every load. Reading the config doesn't leak credentials. |
| `ai::activity_clear` / `ai::activity_list` | `ai.activity.write` | `unrestricted` | The activity log records *what* the AI was asked to do, not the contents — entries are forge-local and don't expose secrets. The settings observability tab needs read access; gating reads would force a cap prompt on every settings open. |
| `security::clear_audit_log` / `security::query_audit_log` | `security.audit.write` | `unrestricted` | The audit log is the operator's record of cap-gated calls and denials. Reading it is exactly what an operator needs to triage; gating reads would defeat the purpose. Truncation is the dangerous direction. |

The pattern: **writes are gated by sensitivity; reads are gated only when the read itself returns sensitive material.** `ai.session.read` / `notifications.inbox.read` exist because session contents and inbox payloads ARE the sensitive material; activity and audit logs index metadata about other operations, not the operations' payloads.

A new pair should follow the same test: if a read returns plaintext secrets / model output / user input, it earns a `.read` cap. If it returns metadata about what happened, it stays `unrestricted` with a `sibling of <write_cap>` note in `cap_matrix.toml`.

## How a plugin gets a capability

```toml
# plugin.toml
[capabilities]
required = ["fs.read", "kv.read"]
optional = ["net.http"]
```

At install:
1. The shell reads `manifest.capabilities` and shows the user the union, with HIGH-risk verbs called out (`shell/src-tauri/src/lib.rs::get_plugin_granted_capabilities`).
2. The user grants a subset. The grant is persisted to `<plugin_dir>/granted_caps.json` (chacha20poly1305-sealed, key in OS keyring per BL-101).
3. At runtime, `nexus-kernel::IpcDispatcher::dispatch` checks each call against the granted set. Missing cap → `CapabilityDenied`.

## Audit trail

Every cap-gated call is logged through `nexus-security::audit` and persisted to `<forge>/.forge/.kernel/audit.db` (90-day retention by default; queryable via `com.nexus.security::query_audit_log`).

## Open questions (AUDIT flags)

`cap_matrix.toml` carries `# AUDIT:` comments for handlers whose current `unrestricted` classification preserves pre-BL-138 behaviour but is a candidate for cap elevation. The full live list is at [`reference/audit-flags.md`](reference/audit-flags.md) (4 entries as of 2026-05-22; 14 historical entries have since been gated and are recorded in the same doc's "Closed since" section).

Live candidates:
- `com.nexus.ai::resolve_credentials` — returns keyring material; currently restricted via `internal = true` (core-trust only) but a future explicit cap is tracked.
- `com.nexus.mcp.host::call_tool` — tool side effects happen in the MCP server's process; `connect`'s `process.spawn` gate bounds who can attach a server in the first place.
- `com.nexus.workflow::{run, run_digest}` — laundering surface; observability shipped (audit-tagged `tracing::warn!` lists implied caller caps on every invocation). Kernel-side enforcement remains the residual gap. Tracked under issue #77 and BL-134 Phase 3; the full threat model + accepted residual risk inventory is in [`amplifier-laundering.md`](amplifier-laundering.md) (#189).
