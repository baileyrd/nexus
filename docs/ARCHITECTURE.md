# Nexus Architecture

> **Moved.** This page used to hold the C4 model; the canonical version
> now lives at [`architecture/C4.md`](architecture/C4.md). The four
> architectural invariants (file-as-truth, microkernel isolation, IPC
> over direct calls, capabilities) live at
> [`architecture/invariants.md`](architecture/invariants.md).
>
> For the live shipped/in-flight state of each subsystem see
> [`PRDs/IMPLEMENTATION_STATUS.md`](PRDs/IMPLEMENTATION_STATUS.md). For
> per-decision context see [`adr/`](adr/).
>
> Other architecture docs (still at the `docs/` root pending mechanical
> `git mv` into `architecture/`):
> - [`leaf-architecture.md`](leaf-architecture.md) — shell pane/leaf model
> - [`editor-transaction-architecture.md`](editor-transaction-architecture.md) — edit-flow model
> - [`ipc-schemas.md`](ipc-schemas.md) — IPC drift-check policy
> - [`legacy-shell-retirement.md`](legacy-shell-retirement.md) — v0.4.0 migration record

This stub exists so that older external links to `docs/ARCHITECTURE.md`
still resolve.
