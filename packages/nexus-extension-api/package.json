{
  "name": "@nexus/extension-api",
  "version": "0.1.0",
  "description": "TypeScript plugin contract for Nexus — common NexusPluginContext + sandbox runtime types (UI F-2.1.1, #187). Re-cut to 0.x per repo-review V9; 1.0.0 returns once the conformance gates have soaked.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "src",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc --noEmit -p tsconfig.json"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  },
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  }
}
