// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.

/**
 * One event in the agent's memory log. Matches PRD-15 §5's
 * `MemoryEntry` enum — variants cover the lifecycle of an agent
 * run plus user-facing feedback.
 */
export type MemoryEntry = { "kind": "user_goal", 
/**
 * Goal text.
 */
text: string, 
/**
 * Unix epoch milliseconds.
 */
timestamp_ms: bigint, } | { "kind": "agent_plan", 
/**
 * Plan identifier (UUID).
 */
plan_id: string, 
/**
 * Number of steps in the plan.
 */
step_count: number, 
/**
 * Unix epoch milliseconds.
 */
timestamp_ms: bigint, } | { "kind": "step_execution", 
/**
 * Step id within the plan.
 */
step_id: string, 
/**
 * Whether the step succeeded.
 */
success: boolean, 
/**
 * Short summary string (truncated to avoid massive logs).
 */
summary: string, 
/**
 * Unix epoch milliseconds.
 */
timestamp_ms: bigint, } | { "kind": "tool_call", 
/**
 * Tool name (matches `AgentToolSpec.name`).
 */
tool: string, 
/**
 * Whether the call succeeded.
 */
success: boolean, 
/**
 * Wall-clock duration in milliseconds.
 */
duration_ms: bigint, 
/**
 * Unix epoch milliseconds.
 */
timestamp_ms: bigint, } | { "kind": "user_feedback", 
/**
 * Feedback text.
 */
text: string, 
/**
 * Unix epoch milliseconds.
 */
timestamp_ms: bigint, } | { "kind": "error", 
/**
 * Error message.
 */
message: string, 
/**
 * Optional step id where the error originated.
 */
step_id: string | null, 
/**
 * Unix epoch milliseconds.
 */
timestamp_ms: bigint, } | { "kind": "decision", 
/**
 * Short summary of the decision.
 */
summary: string, 
/**
 * Free-form rationale text.
 */
rationale: string, 
/**
 * Unix epoch milliseconds.
 */
timestamp_ms: bigint, } | { "kind": "artifact", 
/**
 * Forge-relative path (typically under
 * `<agent_id>/artifacts/`).
 */
path: string, 
/**
 * One-line description.
 */
description: string, 
/**
 * Unix epoch milliseconds.
 */
timestamp_ms: bigint, } | { "kind": "compacted_turns", 
/**
 * How many session rounds the summary replaces. Always
 * `>= 1`; a zero-round compaction is meaningless and
 * callers must not record one.
 */
rounds_compressed: number, 
/**
 * The summary text the compressor produced. Treated as
 * opaque prose by the memory layer.
 */
summary: string, 
/**
 * Unix epoch milliseconds.
 */
timestamp_ms: bigint, };
